const useInputs = () => {
  // TODO: useInput Hook을 작성하라.
  console.log('');
  const onChange = () => {
    // TODO: onChange 함수를 작성하라.
  };
  const reset = () => {
    // TODO: reset 함수를 작성하라.
  };
  return {
    inputs: {},
    onChange,
    reset,
  };
};

export default useInputs;
